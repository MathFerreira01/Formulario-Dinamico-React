import React from "react";

const InputNumero = ({ name, label, id, ordem }) => {

  return (
    <div>
      <label>{label}</label>
      <input name="numero" type="number" id="3" />
    </div>
  );
};

export default InputNumero;
